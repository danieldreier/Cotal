{
  "suite": "extensions/connector-core/smoke/inbox-window.smoke.ts",
  "guard": "cotal_inbox clears only the messages it actually returned, and one response stays inside a receivable window with first-party mail ahead of replayed history",
  "command": "pnpm smoke:inbox-window",
  "completionMarker": "INBOX WINDOW SMOKE",
  "proveWith": "node scripts/mutation-proof.mjs --config extensions/connector-core/smoke/mutations/inbox-window.json",
  "why": [
    "The defect this guards is a composition, so the kills are split along its parts rather than aimed",
    "at one line. K2 restores the drain-the-whole-scope order, K3 removes mail-before-replay, K4 and K7",
    "let peek clear on each path, K5 silences the held-note, K6 acks read-only recall, K11 unbounds the",
    "note, K12 gives stuck mail the waiting mail's promise, K14 unbounds the recall warning, K15 gives a",
    "peeking caller a promise peek cannot keep, and K16 re-serves recall from the same frontier forever.",
    "",
    "K17 through K20 are the recall walk, which this change created and therefore owns. A lane walked",
    "by a single mark can fail in four ways and each has a mutation: comparing times alone strands a",
    "tie, a mark that forgets its id repeats one, stepping over a gap re-serves everything behind it,",
    "and a mark that follows the last item shown strands whatever the gap held. The invariant they",
    "guard together is total progress: every fresh item eventually delivered, and nothing delivered",
    "twice, on inputs including a same-millisecond pair too large to share one window.",
    "",
    "K1, K8, K9 and K10 are the class guard and its edges. Three escapes were found here in sequence,",
    "each the same defect one level further out: an oversized item shown alone, then the head and note",
    "riding outside the budget, then the focus warning appended after the window was filled. Every one",
    "was a writer to the response body the arithmetic did not know about, so the arithmetic was replaced",
    "by assembling the reply and measuring it. K1 disables that measurement, K8 makes an undeliverable",
    "item block the queue, K9 empties the stuck class, and K10 hides a buffer that could deliver nothing.",
    "",
    "SPECIFIER BOUNDARY, checked rather than assumed. The suite imports the agent and the tool specs",
    "by RELATIVE SOURCE PATH (../src/agent.js, ../src/tool-specs.js), so the bytes mutated are the",
    "bytes graded: no dist in the path and no build in the command. Its only package-name import is",
    "`type` from @cotal-ai/core, erased at compile time and carrying no behaviour.",
    "",
    "NOT PROVED HERE, named rather than implied. The 48,000-character budget is a judgement, not a",
    "measurement of any one host's cap: no mutation can show it is the RIGHT number, only that a bound",
    "is enforced and that what falls outside it is kept. An earlier mutation that subtracted the recall",
    "warning from the budget a second time SURVIVED and the code it targeted was deleted rather than",
    "kept: with the reply assembled and measured, no input could tell the two versions apart.",
    "",
    "A SECOND SURVIVOR, resolved the same way as the first. A mutation on a walk that recomputed the",
    "mark over the delivered prefix SURVIVED: the ordered lane already stops at its first gap, so the",
    "last recall item shown IS the end of the prefix and no input could tell the two versions apart.",
    "The walk was deleted rather than the test weakened, and the mark is the last item shown.",
    "",
    "A THIRD SURVIVOR, and this one was a mutation of unreachable code rather than untested code. It",
    "cleared the held gap when an undeliverable message was met, but a strict item after a gap never",
    "reaches that line (the walk skips it earlier) and a buffered item never sorts after a recall gap,",
    "so no input could produce the difference. It was replaced by the mutation above, which turns on the",
    "distinction that line actually makes, rather than kept as a kill the suite could not honestly claim.",
    "",
    "K22 through K25 are the seventh defect, which is #603's own family found inside #603's fix: `ts` is",
    "stamped by the SENDING endpoint, so a mark that walks it is a mark a peer can move. One message",
    "stamped ahead parked the mark in the future and filtered every ordinary message after it out of",
    "recall for the rest of the session, under the reply that there was no chatter. It is a security",
    "property as much as a clock one, so the kills cover both halves of the repair (the split lane and",
    "the mark that only this session's own traffic may move), the record that makes the ahead lane",
    "exactly-once, and the bound on that record. Where the bound's cost lands is the security question,",
    "and cell 24 answers it by measurement: honest traffic never enters the bounded set, so a peer that",
    "spends the whole bound spends it on its own messages and cannot cost another peer its recall.",
    "",
    "K26 through K28 are the eighth defect, which is the seventh's mirror image: the seventh withheld",
    "mail by ORDERING it out of reach, and this one withholds mail by spending the window DESCRIBING",
    "what it is not delivering. A note naming an undeliverable message crowded out a message that fits,",
    "the trim gave back the message rather than the description, and three calls came back identical",
    "while every one of them promised a next batch. The rule the kills defend is that the note yields",
    "before the last message does, in order (names, counts, nothing), and only when it is the last:",
    "while another message still rides along, the full note is worth an item and K26 proves the suite",
    "notices if that boundary moves either way.",
    "",
    "WHAT THIS KILL SET GOT WRONG ABOUT ITSELF, recorded where the next reader will look rather than in a",
    "commit message. Four mutations had to be RE-ANCHORED after the refactors that closed the seventh and",
    "eighth defects moved their targets, and one of those, K1, had SURVIVED: the line it mutated had",
    "stopped deciding anything once the trim was restructured, so the class guard was reporting a kill for",
    "a shape it no longer graded. Two others simply stopped matching, which is loud and harmless. The",
    "quiet one is the lesson: a mutation whose target moves does not fail, it passes for the wrong reason,",
    "and a count of kills cannot tell you which kind you have. The trim is now ONE loop that a single",
    "mutation can turn off, and the note's tier list is authoritative rather than shadowed by a fallback",
    "that ignored it, so both of those mutations grade what their names claim again.",
    "",
    "K29 is the seventh defect's other side, and it is worth naming why it needed a different kind of",
    "cell. The two lanes swap membership when the local clock passes a stamp, which no fixture can",
    "arrange by choosing timestamps: the cell has to wait. It waits 150ms. That is the whole of the",
    "time-dependence in this mechanism, and it is graded rather than reasoned about.",
    "",
    "K30 and K31 are a different axis from every kill above them: not what the walk DOES, but what it",
    "READS. Nine rounds attacked the arithmetic of the window and the mark with well-formed messages,",
    "and one field, the timestamp, turned out to be attacker-controlled. It is not the only one. The",
    "reply is assembled from `text`, `fromName` and `fromRole`, all written by a peer, and rendered into",
    "a structure the caller reads as the tool's own words. The rule these two defend is positional and",
    "checkable by anyone: a line beginning at column zero was written by this tool, and a rendered name",
    "carries neither a closing bracket nor a newline.",
    "",
    "K32 through K37 came from the first adversarial round on the two axes this review had never",
    "attacked, and they say something about the shape of the earlier rounds. Nine of them attacked what",
    "the walk DOES with well-formed messages. When someone finally attacked what it READS, the framing",
    "rule that had just landed for the sender turned out not to hold for the service, the channel, or",
    "either of the two notes, and the mark that had been graded exhaustively within one focus episode",
    "turned out to outlive the episode it belonged to. None of that was hidden by a hard input; it was",
    "hidden by everyone attacking the same axis."
  ],
  "mutations": [
    {
      "name": "K1 the finished response is never measured, only estimated",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  while (text.length > budget && shown.length) {",
      "replace": "  while (false && text.length > budget && shown.length) {",
      "expectRed": "the head line and the held-note are inside the budget, not outside it",
      "cell": "the head line and the held-note are inside the budget, not outside it",
      "note": "THE CLASS GUARD. Three escapes were found here, each a writer to the response body that the arithmetic did not know about. This loop is what replaced the arithmetic: it measures the finished string and gives back trailing items until the real text fits. Disable it and the estimate alone decides again, which is where every one of those escapes lived. The cell that notices first is the near-boundary one: with the measurement disabled the estimate is all that is left, and the estimate is exactly what does not know about the framing."
    },
    {
      "name": "K2 clear the whole scope up front, the order the defect was written in",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "          if (!peek) agent.drainInboxDeliveries(shown.map((i) => i.id));",
      "replace": "          if (!peek) agent.drainInbox(undefined, inboxScope);",
      "expectRed": "what did not fit was NOT consumed: the buffer still holds the rest",
      "cell": "what did not fit was NOT consumed: the buffer still holds the rest",
      "note": "The plausible authoring error rather than a contrived one: draining the scope is what the tool did before, and with a window in place it reads as a harmless simplification."
    },
    {
      "name": "K3 rank every item the same - receive order alone decides the window",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  const rank = (i: InboxItem): number => (i.kind !== \"channel\" ? 0 : i.historical ? 2 : 1);",
      "replace": "  const rank = (_i: InboxItem): number => 0;",
      "expectRed": "the first response is mail, not backfill: DMs occupy the window ahead of replayed history",
      "cell": "the first response is mail, not backfill: DMs occupy the window ahead of replayed history",
      "note": "Without the rank a DM behind a replay waits for as many calls as the replay takes; the loss is gone but the mail is still last."
    },
    {
      "name": "K4 peek clears what it shows",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "          if (!peek) agent.drainInboxDeliveries(shown.map((i) => i.id));",
      "replace": "          agent.drainInboxDeliveries(shown.map((i) => i.id));",
      "expectRed": "peek clears nothing at all",
      "cell": "peek clears nothing at all",
      "note": "peek is the documented workaround for this very defect, so a peek that clears is the worst possible regression here."
    },
    {
      "name": "K5 the response stops saying what it is holding back",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  if (!held.length || tier === \"none\") return \"\";",
      "replace": "  if (held.length >= 0) return \"\";",
      "expectRed": "...and it says what it is holding back",
      "cell": "...and it says what it is holding back",
      "note": "Held mail nobody is told about is mail nobody calls back for - the loss becomes a silent delay instead."
    },
    {
      "name": "K6 the focus drain acks every id in the reply, recall included",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "          agent.drainInboxDeliveries(all.filter((i) => bufferedIds.has(i.id)).map((i) => i.id));",
      "replace": "          agent.drainInboxDeliveries(all.map((i) => i.id));",
      "expectRed": "focus: a recall id was NOT marked handled",
      "cell": "focus: a recall id was NOT marked handled, so a later live copy of it still buffers",
      "note": "The filter reads like defensive noise: the reply carries both lanes, so acking 'what the reply carried' is the obvious simplification. It is not - drainInboxDeliveries marks an absent id HANDLED, so a later live copy of that channel message is dropped as a duplicate, and the read loses mail it never owned."
    },
    {
      "name": "K7 the focus drain stops honouring peek",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "        if (!peek) {\n          agent.drainInboxDeliveries(all.filter((i) => bufferedIds.has(i.id)).map((i) => i.id));",
      "replace": "        if (true) {\n          agent.drainInboxDeliveries(all.filter((i) => bufferedIds.has(i.id)).map((i) => i.id));",
      "cell": "focus + peek: the buffered lane is NOT cleared",
      "expectRed": "focus + peek: the buffered lane is NOT cleared",
      "note": "K4 covers the same slip on the ordinary path only. Without this one a focus reply could clear while peeking, and peek is the workaround this whole change exists to retire, so it has to hold on both paths."
    },
    {
      "name": "K8 one message too large for any response blocks the queue behind it",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "      if (strict && !stuck.has(i.id)) strictGap = true;\n      continue;",
      "replace": "      if (strict && !stuck.has(i.id)) strictGap = true;\n      break;",
      "cell": "ordinary mail is delivered past an item that can never fit",
      "expectRed": "ordinary mail is delivered past an item that can never fit",
      "note": "The fill stops at the first item too large instead of stepping over it, so one message nobody can deliver blocks every message behind it, on both lanes. That is the wedge the show-alone escape was invented to avoid, arriving through the other door."
    },
    {
      "name": "K9 nothing is ever classified as too large to deliver",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "      .filter((i) => opts.head([i]).length + 1 + itemCost(i) + (warning ? warning.length + 2 : 0) > budget)",
      "replace": "      .filter((i) => opts.head([i]).length + 1 + itemCost(i) + (warning ? warning.length + 2 : 0) > budget * 10)",
      "cell": "...and the reply names it rather than leaving it silently stuck",
      "expectRed": "...and the reply names it rather than leaving it silently stuck",
      "note": "Stuck means no response could carry this message. Measure it against a budget nothing can exceed and the class becomes empty: the message is still held, correctly, but the reply calls it ordinary backlog and tells the caller to call again for it forever."
    },
    {
      "name": "K10 nothing shown is reported as an empty inbox, whatever is held",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "    else if (held.length) parts.push(`Nothing could be delivered in this response.${note}`);",
      "replace": "    else if (false) parts.push(`Nothing could be delivered in this response.${note}`);",
      "cell": "...and the reply names it rather than leaving it silently stuck",
      "expectRed": "...and the reply names it rather than leaving it silently stuck",
      "note": "An inbox that says it is empty while holding mail it cannot deliver is the silent version of the same loss: nobody calls back for what they were told is not there. The cell that notices first is the oversized-item cell, because a single oversized message is exactly the case where nothing can be shown; the later empty-inbox cell asserts the same guard from the other side. The cell that notices first is the oversized-item one, because a single oversized message is exactly the case where nothing can be shown; the later empty-inbox cell asserts the same guard from the other side."
    },
    {
      "name": "K11 the note names every oversized message instead of a few",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "      .slice(0, NAMED_STUCK)",
      "replace": "      .slice(0, stuck.length)",
      "cell": "twelve undeliverable messages do not produce twelve lines of metadata",
      "expectRed": "twelve undeliverable messages do not produce twelve lines of metadata",
      "note": "The obvious authoring choice, and the one the second lens flagged before it existed: naming them all is more informative per message and unbounded in aggregate, so a steady stream of oversized mail overflows the reply with metadata about mail it cannot carry."
    },
    {
      "name": "K12 the stuck note borrows the waiting note's promise",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "buffered and uncleared, and calling again will not produce ${stuck.length === 1 ? \"it\" : \"them\"}.`,",
      "replace": "buffered and uncleared. Call cotal_inbox again for the next batch.`,",
      "cell": "...and it does not promise that calling again will deliver them",
      "expectRed": "...and it does not promise that calling again will deliver them",
      "note": "A queue that looks like it is moving when it is not. The caller keeps calling, the same messages keep not arriving, and nothing in the reply says why."
    },
    {
      "name": "K14 the recall warning names every dropped channel",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  const named = channels.slice(0, NAMED_DROPPED).map((c) => `#${attributionSafe(c).slice(0, 40)}`).join(\", \");",
      "replace": "  const named = channels.map((c) => `#${attributionSafe(c).slice(0, 40)}`).join(\", \");",
      "cell": "...and the warning is bounded rather than naming all eighty channels",
      "expectRed": "...and the warning is bounded rather than naming all eighty channels",
      "note": "A caller with many silenced or expired channels is exactly the caller who gets a long list, so an unbounded list is not a rare case: it is the case. With the reply now assembled and measured, an unbounded warning no longer breaks the bound; it eats the window instead, giving back real messages to make room for channel names, which is why the boundedness assertion is the one that catches it."
    },
    {
      "name": "K15 the held note gives a peeking caller the destructive read's promise",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "    const next = peek\n      ? \"A peek clears nothing, so calling again returns this same window; read without peek to take it and see the next.\"\n      : \"Call cotal_inbox again for the next batch.\";",
      "replace": "    const next = \"Call cotal_inbox again for the next batch.\";",
      "cell": "a peek that holds mail does not promise a next batch it cannot deliver",
      "expectRed": "a peek that holds mail does not promise a next batch it cannot deliver",
      "note": "Peek clears nothing, so the next call returns the same window. An obedient caller told to call again loops on it forever, which the first lens measured as three byte-identical replies."
    },
    {
      "name": "K16 focus recall is re-served from the same frontier every call",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "            if (afterRecallMark({ ts: i.ts, id: i.id }, agent.recallCursor)) clocked.push(i);",
      "replace": "            clocked.push(i);",
      "cell": "successive calls walk the whole of focus recall instead of repeating its first window",
      "expectRed": "successive calls walk the whole of focus recall instead of repeating its first window",
      "note": "Reproduced by the second lens on a live broker: recall is re-derived from an unchanged frontier, so windowing its prefix returned the same fifteen of thirty messages three times while every reply promised a next batch. The window created that starvation, so the cursor is part of this change rather than a later one."
    },
    {
      "name": "K17 the fresh filter compares timestamps only, so a tie is skipped",
      "file": "extensions/connector-core/src/agent.ts",
      "find": "  return a.ts !== b.ts ? a.ts > b.ts : a.id > b.id;",
      "replace": "  return a.ts > b.ts;",
      "cell": "a recall twin sharing a millisecond with a delivered item is not filtered out for good",
      "expectRed": "a recall twin sharing a millisecond with a delivered item is not filtered out for good",
      "note": "A timestamp is not a place in a queue when two items share it. Compare on time alone and the held twin is behind the mark forever, while the reply keeps promising a next batch: the starvation direction of the tie."
    },
    {
      "name": "K18 the mark keeps the time and drops the id that breaks its ties",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "          if (last) agent.noteRecalled({ ts: last.ts, id: last.id });",
      "replace": "          if (last) agent.noteRecalled({ ts: last.ts, id: \"\" });",
      "cell": "...and the twin already delivered is not re-served to pay for it",
      "expectRed": "...and the twin already delivered is not re-served to pay for it",
      "note": "The other direction of the same tie: a mark that forgets which item it stopped at leaves the delivered twin ahead of it, so the next call serves it again. Starvation and repetition are the two ways a tie can be got wrong, and the cell asserts against both."
    },
    {
      "name": "K19 the ordered lane steps over its own gap to fit later items",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "    if (strict && strictGap) continue;",
      "replace": "    if (false && strict && strictGap) continue;",
      "cell": "a same-millisecond pair too large to share one window still reaches the caller, both halves",
      "expectRed": "a same-millisecond pair too large to share one window still reaches the caller, both halves",
      "note": "Stepping over a gap looks like better packing: the window is fuller and nothing is lost yet. But a lane walked by one mark cannot record a hole, so everything delivered past it is served again on the next call, forever. The free lane may step over its own gaps because each of its items is acked by id. It reddens on the stranding half of the invariant first: with the gap stepped over, the mark passes the hole and what was in it never comes back, which is the harm. The re-serving half is asserted in the same cell one line later."
    },
    {
      "name": "K21 a message nothing can carry is treated as a gap the walk must wait behind",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "      if (strict && !stuck.has(i.id)) strictGap = true;",
      "replace": "      if (strict) strictGap = true;",
      "cell": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "expectRed": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "note": "The distinction the walk turns on: a message no response could ever carry will never become deliverable, so waiting behind it is waiting forever, while anything else is a real gap and waiting is correct. Collapse the two and one oversized recall message stops the walk for good. No instance cell in this suite puts an undeliverable message in the recall lane; the enumeration does it by construction."
    },
    {
      "name": "K22 a sender's clock is allowed to order the walk again",
      "file": "extensions/connector-core/src/agent.ts",
      "find": "    return item.ts > Date.now();",
      "replace": "    return false;",
      "cell": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "expectRed": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "note": "The defect itself, restored: with every item walked by the mark, one message stamped ahead of the local clock moves the mark past everything the session has not read, and ordinary traffic after it is filtered out of recall for the rest of the session under a reply saying there is none. `ts` is stamped by the sending endpoint, so this is one peer suppressing another peer's recall, not only a clock disagreement. The enumeration is what reddens FIRST now, not the instance cell written after the finding: this defect is reachable from the universe's own inputs, which is the whole point of having enumerated them. The instance cell stays because it is the measured occupant and it says what the numbers were."
    },
    {
      "name": "K23 an item stamped ahead of the clock moves the mark after all",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "          const shownClocked = shownRecall.filter((i) => !aheadIds.has(i.id));",
      "replace": "          const shownClocked = shownRecall;",
      "cell": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "expectRed": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "note": "Splitting the lanes is not enough on its own: if the ahead lane still feeds the mark, the mark ends up in the future anyway and the starvation returns unchanged. Two mutations because the repair has two halves, and either half alone leaves the defect. The enumeration is what reddens FIRST now, not the instance cell written after the finding: this defect is reachable from the universe's own inputs, which is the whole point of having enumerated them. The instance cell stays because it is the measured occupant and it says what the numbers were."
    },
    {
      "name": "K24 the ahead lane hands over without recording what it handed over",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "          for (const i of shownRecall) if (aheadIds.has(i.id)) agent.noteRecalledAhead(i.id);",
      "replace": "",
      "cell": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "expectRed": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "note": "The ahead lane has no mark, so the record BY ID is the only thing that makes it exactly-once. Without it every call hands the same future-stamped message over again, which is the duplicate failure that any repair clamping the timestamp instead would also produce. The enumeration is what reddens FIRST now, not the instance cell written after the finding: this defect is reachable from the universe's own inputs, which is the whole point of having enumerated them. The instance cell stays because it is the measured occupant and it says what the numbers were."
    },
    {
      "name": "K25 the ahead lane hands over more than it can remember",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "          if (aheadRoom <= 0) aheadWithheld.push(i);",
      "replace": "          if (false) aheadWithheld.push(i);",
      "cell": "...and what it will not take responsibility for, it names instead of dropping quietly",
      "expectRed": "...and what it will not take responsibility for, it names instead of dropping quietly",
      "note": "The memory that makes the ahead lane exact is bounded, because it is exactly what a flood of forged stamps would grow. Past the bound the reply must SAY the messages are not being handed over: show them without a place to record them and they come back on every call forever, silently, which is the failure the bound exists to avoid."
    },
    {
      "name": "K26 the trim gives back the last message rather than shortening the note",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "    if (shown.length === 1) {",
      "replace": "    if (false) {",
      "cell": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "expectRed": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "note": "The eighth defect exactly: with the loop free to empty the response, a message that renders inside the window on its own is given back to make room for a note NAMING a message that can never be delivered, and the next call rebuilds the same reply forever. The bound on the loop is what makes the last message win. The enumeration is what reddens FIRST now, not the instance cell written after the finding: this defect is reachable from the universe's own inputs, which is the whole point of having enumerated them. The instance cell stays because it is the measured occupant and it says what the numbers were."
    },
    {
      "name": "K27 the note yields the window while other messages are still riding in the response",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "    if (shown.length === 1) {\n      const yielded = fit(shown, held);",
      "replace": "    if (shown.length >= 1) {\n      const yielded = fit(shown, held);",
      "cell": "...and that case really did exercise all three: it cleared mail, held mail, and warned",
      "expectRed": "...and that case really did exercise all three: it cleared mail, held mail, and warned",
      "note": "The boundary is a trade, not a preference, so it is graded in both directions. Yield too early and a packed response stops NAMING the mail it cannot deliver even though giving back one small item would have paid for those names; yield too late, which K26 covers, and the last message is given back and nothing is delivered at all."
    },
    {
      "name": "K28 the note gives up its names but never gives up existing",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "const NOTE_TIERS: readonly NoteTier[] = [\"full\", \"compact\", \"none\"];",
      "replace": "const NOTE_TIERS: readonly NoteTier[] = [\"full\", \"compact\"];",
      "cell": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "expectRed": "every scenario in the cursor's input universe makes total progress with no duplicates",
      "note": "Shortening the note closes the measured case but not the class: a message can be large enough that not even a count fits beside it, and then counting is still chosen over delivering. Nothing is lost by dropping the note, because the held mail is still buffered, still uncleared, and named by the very next reply, which the same cell asserts. The enumeration is what reddens FIRST now, not the instance cell written after the finding: this defect is reachable from the universe's own inputs, which is the whole point of having enumerated them. The instance cell stays because it is the measured occupant and it says what the numbers were."
    },
    {
      "name": "K29 an item that decays out of the ahead lane is handed over a second time",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "            if (agent.recallAheadSeen(i.id)) continue;\n            if (afterRecallMark({ ts: i.ts, id: i.id }, agent.recallCursor)) clocked.push(i);",
      "replace": "            if (afterRecallMark({ ts: i.ts, id: i.id }, agent.recallCursor)) clocked.push(i);",
      "cell": "...and crossing into the clocked lane does not hand it over a second time",
      "expectRed": "...and crossing into the clocked lane does not hand it over a second time",
      "note": "Found by the first lens attacking the seventh fix from the other side. The mark never moves for an item handed over while it was ahead of the clock, so when the clock passes that stamp the item arrives in the clocked lane above the mark and is offered again. The lanes are kept honest with each other by the same record that makes the ahead lane exactly-once. Its cell is the only one in this suite that WAITS for real time, because the crossing cannot be staged by stamping the clock."
    },
    {
      "name": "K30 a peer's newlines are rendered as the reply's own lines again",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  return text.replace(LINE_BREAK, \"\\n  \");",
      "replace": "  return text;",
      "cell": "a peer's newlines cannot add a line the reply appears to have written itself",
      "expectRed": "a peer's newlines cannot add a line the reply appears to have written itself",
      "note": "The reply is structured and assembled entirely from peer-controlled text, so without the continuation indent a single message writes that structure itself: measured, one message forged a second message line attributed to another named peer, the held-note with its call-again promise, and the recall warning. Indentation is the only thing separating what the tool said from what a peer said it said."
    },
    {
      "name": "K31 a peer may close the bracket it is named inside",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  return s.replace(/[\\r\\n\\v\\f\\u0085\\u2028\\u2029\\]]+/g, \" \");",
      "replace": "  return s;",
      "cell": "...and a peer cannot close the bracket it is named inside",
      "expectRed": "...and a peer cannot close the bracket it is named inside",
      "note": "Attribution is rendered inside brackets on every surface that carries it, this tool's reply and the connectors' wake hints alike, so a name holding a closing bracket or a newline ends the attribution early and starts writing the surface's own syntax. `fmtFrom` is the shared primitive, which is why the neutralization lives there rather than at one call site."
    },
    {
      "name": "K32 a service name is rendered as the publisher wrote it",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  if (i.kind === \"anycast\") return `[@${attributionSafe(i.service ?? \"\")} from ${fmtFrom(i)}] ${body}`;",
      "replace": "  if (i.kind === \"anycast\") return `[@${i.service ?? \"\"} from ${fmtFrom(i)}] ${body}`;",
      "cell": "a service name cannot close its own bracket or start a line",
      "expectRed": "a service name cannot close its own bracket or start a line",
      "note": "`toService` is written by the publisher and is not checked against the subject the message arrived on, so a raw publisher on the real role subject sets it freely, and it is rendered inside the same brackets as the sender. Found by the third lens after the framing rule had already landed for the sender: the rule was stated absolutely and this field did not obey it."
    },
    {
      "name": "K33 a channel label is rendered as it arrived",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  return `[#${attributionSafe(i.channel ?? \"\")}${i.mentionsMe ? \" @you\" : \"\"} ${fmtFrom(i)}] ${body}`;",
      "replace": "  return `[#${i.channel ?? \"\"}${i.mentionsMe ? \" @you\" : \"\"} ${fmtFrom(i)}] ${body}`;",
      "cell": "...and neither can a channel label, whatever validated it upstream",
      "expectRed": "...and neither can a channel label, whatever validated it upstream",
      "note": "The official receive paths rewrite a channel label from the subject token, so this one is defence in depth rather than a measured live hole. It is here because the rule is positional and absolute: a renderer that holds only while every upstream path validates correctly is a renderer whose guarantee is somebody else's."
    },
    {
      "name": "K34 the future-stamp note names senders as they name themselves",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  const senders = [...new Set(items.map((i) => attributionSafe(i.fromName).slice(0, 40)))];",
      "replace": "  const senders = [...new Set(items.map((i) => i.fromName.slice(0, 40)))];",
      "cell": "the note naming withheld senders cannot be split into a forged line by one of them",
      "expectRed": "the note naming withheld senders cannot be split into a forged line by one of them",
      "note": "Measured by the third lens: a withheld sender calling itself `Ada]` plus a newline split the warning in two and put a forged attribution at column zero. A note is not a lesser surface than a message line; it is the part of the reply a caller most reads as the tool speaking."
    },
    {
      "name": "K35 the recall warning names channels as they arrived",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "  const named = channels.slice(0, NAMED_DROPPED).map((c) => `#${attributionSafe(c).slice(0, 40)}`).join(\", \");",
      "replace": "  const named = channels.slice(0, NAMED_DROPPED).map((c) => `#${c.slice(0, 40)}`).join(\", \");",
      "cell": "...and neither can the recall warning, by a channel it names",
      "expectRed": "...and neither can the recall warning, by a channel it names",
      "note": "The second note, and the same rule. Joined channel names are validated upstream, so like K33 this is defence in depth at the render boundary rather than a measured live hole."
    },
    {
      "name": "K36 only the line breaks JavaScript splits on are treated as line breaks",
      "file": "extensions/connector-core/src/tool-specs.ts",
      "find": "const LINE_BREAK = /\\r\\n?|[\\n\\v\\f\\u0085\\u2028\\u2029]/g;",
      "replace": "const LINE_BREAK = /\\r\\n?|\\n/g;",
      "cell": "a U+2028 separator cannot put an attribution at column zero either",
      "expectRed": "a U+2028 separator cannot put an attribution at column zero either",
      "note": "Measured by the third lens through the host frame a model is handed, an MCP text part stringified and parsed back: U+2028, U+2029 and U+0085 survive transport intact and sit directly before an unindented attribution. A JavaScript split does not see a line there and neither does `wc -l`; a Unicode-aware splitter does. NOT PROVED HERE, and the lens said so first: no model transcript was opened, so this is not a measurement that a model rendered a break. The class was widened because the rule is stated absolutely, not because a reader was watched."
    },
    {
      "name": "K37 the recall mark outlives the frontier it was walking",
      "file": "extensions/connector-core/src/agent.ts",
      "find": "  private resetRecallWalk(): void {\n    this._recallCursor = { ts: 0, id: \"\" };\n    this.aheadDelivered.clear();",
      "replace": "  private resetRecallWalk(): void {\n    if (this._recallCursor.ts < 0) this.aheadDelivered.clear();",
      "cell": "...and leaving focus forgets where that walk had read",
      "expectRed": "...and leaving focus forgets where that walk had read",
      "note": "The mark describes a position in ONE walk over ONE frontier, and focus drops the frontier on the way out and captures a new one on the way in. Measured by the third lens on a single incarnation: a mark left from an earlier episode filtered a new episode's messages out of recall whenever they were stamped behind it, which a lagging or a chosen clock produces. The focus watermark was already cleared here; the mark that walks it was not."
    }
  ]
}
