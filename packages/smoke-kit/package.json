{
  "name": "@cotal-ai/smoke-kit",
  "description": "Test-only helpers shared by the smoke suites. Private, never published, never imported by src/.",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "tsx smoke/package.smoke.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
