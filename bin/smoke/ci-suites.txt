# The smoke:ci chain, in execution order: one script name per line.
#
# THIS FILE IS THE CHAIN. It was a single ~6500-character `smoke:ci` string in package.json, and
# that shape had one property that cost real time: every branch adding a suite appended to the same
# line, so any two such branches conflicted with each other, and the conflict landed inside a JSON
# string where a resolution drops or duplicates an entry without looking wrong. Adding a suite is
# now adding a line.
#
# Blank lines and `#` comments are ignored. Every other line must be a script defined in
# package.json - `smoke:gate-inventory` checks that, checks nothing here is duplicated, and checks
# the file is non-empty, so a chain that has quietly stopped running suites cannot read as a chain
# that passes.
#
# ORDER IS EXECUTION ORDER and it is not alphabetical: `smoke:dist-freshness` runs first because a
# stale build makes every later result a statement about code that is not in the tree. This legacy
# list is now FROZEN; add new suites one-file-per-suite under `ci-suites.d/`.
smoke:dist-freshness
smoke:gate-inventory
smoke:dep-pins
smoke:principal-key
smoke:grammar
smoke:ep-grammar
smoke:ep-grants
smoke:frozen-exports
smoke:canonical
smoke:schema-profile
smoke:contract-vocabulary
smoke:journal-declaration
smoke:journal-model-b
smoke:safe-pattern-fuzz
smoke:ep-envelope
smoke:broker-floor
smoke:owner-token
smoke:auth-owner-token
smoke:auth-issuer
smoke:auth-idp-bridge
smoke:auth-login
smoke:auth-attenuation
smoke:callout-shape
smoke:auth-callout:auth
smoke:endpoint-usermode:auth
smoke:callout-deny:auth
smoke:session-adapter:auth
smoke:lifecycle-registry:auth
smoke:credential-ledger:auth
smoke:admission-mediator:auth
smoke:retirement-barrier:auth
smoke:retirement-cleaner:auth
smoke:mediator-confinement:auth
smoke:deny-new:auth
smoke:connect-reader-acl:auth
smoke:shape-supervisor:auth
smoke:descendant-deny:auth
smoke:barrier-plane:auth
smoke:plane-claim:auth
smoke:drain-repair:auth
smoke:auth-admin:auth
smoke:auth-pidfile
smoke:d32-matrix:auth
smoke:session-caller-grant
smoke:session-serving-grant
smoke:eps-grant-sweep
smoke:storage-authority:auth
smoke:read-acl:auth
smoke:acl-cas
smoke:read-history:auth
smoke:kv-scan
smoke:kv-read-shape
smoke:history-recent
smoke:ep-records
smoke:ep-journal
smoke:ep-binding
smoke:ep-work
smoke:ep-virtual
smoke:ep-action
smoke:ep-action-class
smoke:ep-goaleff
smoke:ep-epname
smoke:ep-effects
smoke:goal-epoch-fence
smoke:ep-checkpoint
smoke:ep-handle
smoke:ep-session
smoke:ep-guard
smoke:ep-receipt
smoke:ep-contract-store
smoke:ep-invoke
smoke:ep-serve
smoke:ep-serve-gate
smoke:bind-fence
smoke:unfenced-responder
smoke:manager-service
smoke:manager-service-authority
smoke:manager-service-ops
smoke:manager-service-invoke
smoke:manager-spawn-action
smoke:manager-spawn-action-auth
smoke:mesh-attach
smoke:attach-repaint
smoke:mesh-attach-plane
smoke:session-cap
smoke:session-ledger-family
smoke:sibling-mint-fence
smoke:secret-store-seam
smoke:console-ws-listener
smoke:console-session-route
smoke:console-ws-duplex
smoke:manager-restart-fence
smoke:manager-scatter
smoke:manager-multi-live
smoke:manager-lease-probe
smoke:inst-route-grant
smoke:inst-route-enforce
smoke:manager-lease-grant
smoke:manager-coexist
smoke:manager-name-claim
smoke:manager-reconcile-ownership
smoke:manager-on-route
smoke:static-lifecycle
smoke:manager-restart-live
smoke:preserve-state
smoke:ep-serve:auth
smoke:ep-verbs
smoke:ep-traits
smoke:sub-acl:auth
smoke:self-serve-join:auth
smoke:control-auth
smoke:manager-split
smoke:supervise-registered-user
smoke:supervise-registered-user-authority
smoke:cred-lifetime
smoke:standing-renewal
smoke:delivery-renewal
smoke:signing-key-rotation
smoke:system-account-rotation
smoke:cutover-restart-eviction
smoke:deprovision
smoke:channels:auth
smoke:e2e:acl
smoke:cross-owner:auth
smoke:plane3:auth
smoke:delivery-lease:auth
smoke:delivery-leave-tombstone:auth
smoke:delivery-reconnect:auth
smoke:delivery-cred:auth
smoke:delivery-reply-injection:auth
smoke:evict-live:auth
smoke:membership:auth
smoke:membership-feed-confinement:auth
smoke:wildcard-backfill
smoke:opencode
smoke:opencode-coop
smoke:codex-args
smoke:codex-host
smoke:codex-dist
smoke:codex-installed
smoke:jcode-args
smoke:jcode-host
smoke:jcode-private-state
smoke:jcode-security
smoke:web-harness
smoke:web-parts
smoke:web-channel-authority
smoke:persona-acl
smoke:cli-kernel
smoke:join-creds
smoke:flag-inventory
smoke:start-overrides
smoke:launch-parity
smoke:seat-env-scope
smoke:suite-ambient-env
smoke:spawn-args
smoke:own-agent-control
smoke:views
smoke:agent-file

# Every persona this repo SHIPS declares the channels it reads, and the set of code paths that write
# into the persona catalog is enumerated by output path rather than by function name. The writer
# refuses a definition with no read set, but setup writes its personas as literal template strings
# and never reaches it, so a template that forgot the field would ship an agent whose read set is
# whatever the current default happens to be.
smoke:persona-templates
smoke:card-host
smoke:attach-auth
smoke:attach-host-persistence
smoke:launch-options
smoke:clean
smoke:multi-space
smoke:meshes-registry
smoke:mesh-root-identity
smoke:canonical-root-missing
smoke:mesh-target-rebind
smoke:join-target
smoke:join-dial-entry
smoke:down-target
smoke:ledger
smoke:down-lease-tombstone
smoke:spawn-from-anywhere
smoke:spawn-from-anywhere:live
smoke:cross-path-dedup
smoke:inbox-window
smoke:backup-cli
smoke:registry-staging
smoke:materialize-concurrency
smoke:seed
smoke:setup-failloud
smoke:agent-skills
smoke:ext-seed-help
smoke:secret-store
smoke:space-auth-store
smoke:delivery-store-boundary
smoke:delivery-shards-reject
smoke:delivery-teardown
smoke:auth-store
smoke:adoption-proof
smoke:adoption-swap-strand
smoke:adoption-passive-preflight
smoke:renewal-record-honesty
smoke:component-health
smoke:reload-deadline-queue
smoke:adoption-doctor-e2e
smoke:membership-rw-renewal
smoke:ps-operator-path
smoke:ps-user-mode
smoke:claude-wake
smoke:tls-serve
smoke:up-tls-routes
smoke:broker-policy
smoke:serverconfig-shapes
smoke:tls-intent
smoke:lang-grammar
smoke:lang-keys
smoke:lang-journal
smoke:lang-sim
smoke:lang-interpret
smoke:lang-fuel
smoke:lang-dryrun
smoke:lang-examples
smoke:lang-options
smoke:doctor-auth
smoke:sys-rotation
smoke:sys-rotation-e2e
smoke:persona-input-closed
smoke:tool-input-closed
smoke:opencode-tool-closed
smoke:pi-tool-closed
smoke:hermes-tool-closed
smoke:hermes-hooks-control
smoke:codex-tool-closed
smoke:control-reply
smoke:manager-invoke-verdict
smoke:goal-sibling-race
smoke:pid-contract
smoke:artifact-contract
smoke:artifact-store
smoke:backup-inventory
smoke:probe-teardown
smoke:persona-announce
smoke:presence-ttl-migration:auth
smoke:presence-ttl-refresh-cli
smoke:presence-ttl-expiry-open
smoke:retire-reply-bind
smoke:gate-reconcile:auth
smoke:gate-reconcile-cli:auth
smoke:resolve-rtt
smoke:queue-win
smoke:instrument-instance-pin
smoke:cli-on-instance
smoke:cli-seat-locality
smoke:web-registration-seam
smoke:mint-provision:auth
smoke:ep-rail-failure
smoke:bind-fence:live
smoke:claude-launch-model
smoke:ci-suites
smoke:membership-feed:auth
smoke:describe-split-effect
smoke:web-membership-refusal
smoke:cas-publish
smoke:cas-preflight-cluster
smoke:frame-size
smoke:valid-name
smoke:actor-ephemeral
smoke:principal-channel-witness
smoke:part-renderer
smoke:auth
smoke:mutation-fixtures
smoke:agui-conformance
smoke:agui-kind
smoke:lease-renew
smoke:event-channel
smoke:event-channel-resolve
smoke:agui-split
smoke:durable-source
smoke:event-wal
smoke:agui-wal-path
smoke:agui-emitter
smoke:agui-retry-duplicate
smoke:agui-holder
smoke:opencode-boot-prompt
smoke:import-remedy
smoke:broker-teardown
smoke:event-channel-classify
smoke:agui-render
smoke:agui-render-parity
smoke:agui-frame-dispatch
smoke:view-events-filter
smoke:event-order
smoke:web-events-filter
smoke:reaper
smoke:scatter-liveness
smoke:service-dereg
smoke:manager-deregister
smoke:scatter-pinned-probe
smoke:agui-close-run
smoke:claude-events-arm
smoke:claude-run-error
smoke:events-grant
smoke:spawn-foreground-events
smoke:seat-input
smoke:user-spawn:live
smoke:agui-authorship
smoke:agui-map
smoke:attach-reconnect
smoke:agui-multi-session
smoke:subject-frontier

# The cotal-lang runtime: the language package, the durable step journal and run driver, and the
# mesh handler that binds the effect interface onto the real planes.
smoke:mutation-proof
smoke:doc-binding
smoke:run-journal
smoke:run-notice
smoke:run-migration
smoke:run-journal-auth
smoke:lang-pins
smoke:lang-scopes
smoke:lang-notify-fact
smoke:lang-migrate
smoke:runtime-journal-store
smoke:runtime-run-driver
smoke:runtime-mesh-sleep
smoke:runtime-mesh-checkpoint
smoke:runtime-mesh-wait
smoke:runtime-mesh-notify
smoke:runtime-mesh-seam
smoke:runtime-migrate
smoke:runtime-fork
smoke:lang-semantics
smoke:lang-surface

# The OpenCode connector's AG-UI event plane: the durable read, the mapper, the launch that
# arms it, and the per-session emitter swap that keeps `/new` from going dark.
smoke:agui-opencode-map
smoke:agui-opencode-source
smoke:opencode-events-arm
smoke:opencode-events-reset
smoke:opencode-run-error
# Beside the swap rather than at the end, because it grades the other half of the same event: what
# that session leaves on disk once the wire is settled. It spawns two processes of its own and holds
# one drain past the swap bound on purpose, so it is the slower of the pair.
smoke:opencode-events-release
smoke:goal-follow
smoke:required-arg-seam
smoke:attach-stdin

# The Codex connector's AG-UI event plane: the mapper against a real app-server
# thread, the launch arm, and the per-agent rollout resolution.
smoke:agui-codex-map
smoke:codex-events-arm
smoke:codex-rollout-path
# The same plane through the seam a spawn actually takes: a real broker, the real host process, a
# real rollout on disk, and a subscriber reading the frames off the channel.
smoke:codex-events-lifecycle

# The web dashboard's resilience to a link that stalls: what the pages keep when a poll refuses,
# what the activity aggregation returns when it runs out of time, and whether a presence watch
# rebuild can clean up after itself.
smoke:web-snapshot
smoke:web-bounded-aggregation
smoke:presence-watch-rebuild:auth
smoke:web-history-limit
smoke:core-history-limit
smoke:web-graph-boot
smoke:web-echo-escaping
smoke:web-channel-alias
# The engine host: the seam the transformed module is handed, the worker that
# runs it under lockdown in a Compartment with zero endowments, and the laws the walker enforces
# inline enforced here at one boundary instead.
smoke:lang-engine

# The source transform: the emitted module's surface, closed over the seam,
# reaching no unruled member, pure in its source, one rule per admitted node type. The differential
# suite against the walker is the primary gate and lands with the engine host's `__ctx`.
smoke:lang-transform

# The primary gate for the two engines: the same program through the walker and through the engine,
# compared on value, log, refusal and journal entry for entry. What a rule made divergent, and
# what the engine cannot run yet, are two named lists inside it rather than programs left out.
smoke:lang-differential

# The two auth suites this branch revived: the despawn/respawn barrier contract (reservation,
# lifecycle keying, replay safety) and the swallowed-revoke name hold. Both threw before their
# first cell for a month while ungated, which is how a retry budget drifted past a presence TTL
# with nothing to report it.
smoke:freeslot-barrier:live
smoke:int2-revoke:live
smoke:operator-env-keep
smoke:spawn-env-config
smoke:spawn-env-e2e
smoke:hermes-launch-env
smoke:env-isolate

# `cotal attach` redeems with the seed the MESH resolved, never one walked up from the cwd (#722):
# a real authed broker that trusts one chain, a fossil `.cotal` holding another, and the binary run
# from each directory in turn. Gated because the regression it covers was invisible to every
# existing suite: they all run from a checkout that IS the resolved root, where the two answers
# agree.
smoke:attach-auth-root
smoke:manager-reconcile-startup

# Historical channel ambient must never drive automatic turns (#775). Found on the first real
# mesh join a jcode seat ever made: the live E2E mesh is seconds old, so no suite had ever
# delivered historical=true into a host-mode drive loop, and the join backfill arrived as a
# storm of user turns (119 digests, 0 assistant output, provider emergency compaction).
smoke:history-flood

# The delete route reads its request body under a hard cap so an unbounded client cannot balloon the process (#721).
smoke:web-body-cap

# The block that carries peer messages into a turn must name the order of operations, not just the
# reply verbs (#782). Found by dogfooding a live jcode seat: told to write a file and confirm, it
# sent the confirmation five seconds later with zero file tool calls and no file on disk, twice.
# The old tail offered three ways to reply and never mentioned doing the work, at the exact moment
# the model chooses its next action.
smoke:injection-framing

# The inbox overflow valve must not let channel volume silence directed mail (#791). pullOnly is
# `!mentionsMe && historical` and mentionsMe comes from a forgeable payload field, so a peer that
# stamped the victim's name on every flooded message made none of its traffic pull-only: eviction
# fell through to the oldest entry, a waiting DM, and acked it unhandled. Plain ambient volume did
# the same with no forgery. The suite shipped with #776 buffered 120 against MAX_INBOX 200, so it
# never crossed this valve at all.
smoke:inbox-overflow-directed

# The optional remote-exchange face: a real auth-service with both listeners proves the public
# four-route allowlist, capless credential proof, loopback negative control, immediate revocation,
# public-only view refusal, Origin/body hardening, peer isolation and bearer refresh after expiry.
smoke:remote-exchange:live

# A remotely enrolled managed agent uses its already-issued actor token + sentinel to mint through
# the pinned public exchange, then connects and publishes on the real broker with no local auth service.
smoke:remote-agent-bearer:live

# A restarted seat must resume the session it left, not silently start blank (#789). The host called
# createSession unconditionally, so stop+respawn forked a new session and orphaned the transcript:
# a live seat answered NO PRIOR CONTEXT about work in its own 112 KB journal, while the TUI, spawned
# with --resume, showed a human the history the agent could not recall.
smoke:jcode-session-resume

# A seat must name the provider actually carrying its model, and refuse a provider-prefixed model id
# at the boundary (#785). Measured: one route reported three identities - requested gemini-3.7-flash
# under a cliproxy default, logged as prv:OpenRouter, died in a cursor_plugin_error - and none of the
# three appeared in the roster, the spawn confirmation, or the manager exit line. RuntimeInfo already
# carried provider and routes in the response the model check reads; they were discarded.
smoke:jcode-route-identity

# A provider stream stall can close the Jcode Harness API during an inbox-driven turn. The connector
# must preserve the mesh seat and unacknowledged batch, make exactly one private replacement, attach
# the same session, and accept subsequent mesh work; a failed recovery exits rather than looping.
smoke:jcode-provider-disconnect

smoke:orientation

# A signing key must not expire unattended. The manager minted its session key once at boot with a
# flat 24h window and handed back that frozen anchor forever, so any manager with >24h uptime lost
# its session plane: every attach failed closed with "outside its validity window", recoverable only
# by restarting the manager, which kills every live session. It happened three times in one day.
# Failing closed is correct (SPEC 13.10); never renewing is the defect. Rotate early, keep the old
# key verifiable across an overlap, and never drop the newest.
smoke:anchor-renewal

# A connector must never return — success or failure — while the private Jcode tree it launched is
# still executing. The SDK's managed stop keys the daemon off a servers.json entry matching the
# alias socket path verbatim, so a canonicalized entry or wiped registry is a silent no-op, and the
# setsid daemon (plus its MCP child) outlives the seat: an unmanaged orphan holding the seat's
# private home. The connector now proves the teardown from the seat's own records — bounded SIGTERM,
# exact-PID/group SIGKILL escalation, throw rather than report clean when a PID survives.
smoke:jcode-lifecycle
# The un-ack reprieve has to end (#807). #793 stopped the overflow valve acking a directed message
# it evicts so the broker can redeliver it once there is room - but an un-acked id can be handed
# straight back into a still-full inbox and evicted again, forever, spending throughput while every
# seat reports healthy. Same shape as #790 one layer down. Count evictions per id, give up after
# five, and say so on stderr (never emit("error") - with no listener that kills the seat).
smoke:overflow-churn-bound

# The auth-service serves the mesh discovery document `cotal meshes add --from` registers from, and
# nothing had ever fed the server's ACTUAL bytes to that consumer: the producer's smoke asserted the
# fields it had just written, and the consumer's smoke fed itself a hand-written fixture. The shape
# defect that hid there (a flat document with no `userAuth` and no `provider`, refused with "a
# provider name is required") is fixed, but the untested seam is the reason it shipped. This suite
# is the crossing: live daemon, fetch over the wire, raw bytes into the shipped consumer — and it
# grades the served provider name against the REGISTERED provider's own name, so the two cannot
# drift back apart behind a re-hardcoded literal.
smoke:discovery-bundle-consumable:live
# A failed jcode turn must not become a hot loop (#790). The batch is acked only on success, so a
# failure leaves pendingWake() positive and the finally re-drives the SAME batch instantly - one
# stalled turn re-paid the full injection 62 times. The codex host has had scheduleErrorRetry the
# whole time; this grades the same rule for jcode: growing delay, ceiling, one timer, give up.
smoke:jcode-retry-policy
# When a seat leaves, the manager must say WHY. A live supervisor lost several seats while it kept
# running and its log carried no per-seat exit line at all, so "the supervisor reaped them" and
# "they died on their own" were indistinguishable after the fact. Two different facts are required:
# which path gave up the slot, and what the runtime saw when the child ended — and a runtime that
# cannot see the exit must say so rather than print a fabricated code 0.
smoke:seat-exit-cause
smoke:empty-id-ingest
smoke:hermes-empty-id
# A manager never ends its own process over its liveness lease. The old fail-close turned a ten-second
# broker or KV outage into a manager outage that lasted until an operator noticed (netcup, 2026-08-27,
# ten hours). This entry replaces `smoke:detach-not-teardown` IN PLACE, at the same index, so no other
# suite changes shard; the in-process half of the guard is here, the wire half is `smoke:lease-renew`.
smoke:lease-loss-keeps-serving
# Appended at the end per this file's own rule: the cell has no ordering dependency, and appending
# is what keeps two branches adding suites from conflicting. It is the end-to-end half of a guard
# whose ingest half runs earlier as `smoke:cross-path-dedup`: that one drives hand-built deliveries
# into the dedup branch, this one proves the CONNECTOR's boot window recovers over a real broker.
smoke:claude-boot-wake
# Control commands resolve ws:// / wss:// meshes too. Exercise the real CLI against one broker's
# websocket and TCP listeners so raw node-transport regressions name their `wsconnect` refusal.
smoke:control-transport-dial
# A manager restart killed mid-barrier left the issuance gate frozen, and the successor refused
# SPEC 13.8 until an operator ran reconcile-gate (#783 item 3 / #871). Boot now completes that
# same dead registration when the freeze-holder is gone under a complete CONNZ sweep. Appended
# at the end: no ordering dependency, and appending keeps two branches from conflicting.
smoke:boot-self-heal-gate
# A Claude seat must receive CLAUDE_CODE_OAUTH_TOKEN (the credential docs/deploy.md promises) and
# must not receive CLAUDE_CODE_CHILD_SESSION. The allowlist that closed #866 dropped the token
# because this connector declared no providerKeys; a container has no Keychain to fall back to.
smoke:claude-launch-env
smoke:web-console-auth
smoke:no-implicit-general
smoke:session-channels
smoke:jcode-private-lifecycle
smoke:hook-load-emitter-race
smoke:spawn-name-actor
# Claude writes a positional startup prompt before SessionStart reaches the connector. A generic
# fresh adopt then parks after the only run opener and all later assistant/tool records map to zero.
# `source: startup` reads this genuinely-new session from byte zero; resume/fork/clear/compact still
# adopt retained history at the current boundary, and a recovered WAL cursor always wins. Appended
# because this suite has no ordering dependency and append-only additions preserve shard membership.
smoke:claude-start-source
# Claude can fire SessionStart before the MCP process binds its local control socket. The real relay
# must survive that first refusal inside its existing two-second budget, then deliver exactly once;
# an absent connector still fails open at the deadline. Appended to preserve every existing shard.
smoke:hook-relay-startup
# Restore recreates excluded endpoint infrastructure through the production creation seam. That
# needs UPDATE on the records KV hardening step, but no restored message stream may inherit UPDATE;
# the infrastructure credential's exact subset is gated independently from the live restore cycle.
# Appended so existing shard membership stays stable.
smoke:isolated-infrastructure-scope
# Channel registry/replay now uses OS-assigned ports, polls positive readiness, and awaits broker
# teardown. Four concurrent real-broker runs pass without port collision or stale state; append to
# preserve every existing shard assignment.
smoke:channels
# A managed-static renewal accepted before terminalization publishes an in-flight barrier; the
# terminal drains it before durable revocation and file cleanup, while arrivals after the latch are
# refused. Four fresh lifecycles drive both orderings against a real JWT broker and journal.
# Appended to preserve every existing shard assignment.
smoke:renewal-terminal-race
# The one-shot send smoke now owns an OS-assigned open broker, passes that address to its receiver
# and every real CLI subprocess, and resolves the repository's actual bin/cotal.ts entry. Concurrent
# runs therefore share neither port nor state. Appended to preserve every existing shard assignment.
smoke:send
# Membership feature coverage now owns an OS-assigned open broker, provisions each unique space
# through setupSpaceStreams, tears it down through deleteSpace, and uses only current token-safe ids.
# Concurrent runs share neither port nor space. Appended to preserve every existing shard assignment.
smoke:membership
# These seven self-contained suites are cited by the shipped plan as executable evidence. Each owns
# or needs no broker, passed serially and in six-way shard-like overlap, and was previously run by
# nothing. Appended as one debt slice so every existing shard assignment remains unchanged.
smoke:channel-attention
smoke:channel-attention:auth
smoke:install
smoke:manifest-launch
smoke:members
smoke:presence-scrub
smoke:start-model
# The bare core smoke now owns an OS-assigned broker and a production-provisioned unique space. It
# drives explicit live channels, lifecycle-scoped offline DM restart delivery, pre-activation DM
# exclusion, presence, and open-mode membership semantics, then awaits broker teardown. Appended so
# every existing shard assignment remains unchanged.
smoke
# The callout-minted control-trust proof owns a JWT broker and two user principals, then proves
# payload, same-owner subject, cross-owner subject, and reply-lane forgeries all fail on the shipped
# delivery control rail. Appended to preserve every existing shard assignment.
smoke:ctl-trust:live
# The static-auth dashboard seed-drop proof owns a JWT broker and registered workspace, seeds chat
# through the operator profile, then proves its pre-minted channel-purger can purge while admin
# cannot. Appended to preserve every existing shard assignment.
smoke:web-seed:live
# The persona harness reachability proof drives foreground and detached spawn through a throwaway
# broker, including a caller-only COTAL_DEFAULT_AGENT crossing into an already-running manager.
# Appended to preserve every existing shard assignment.
smoke:persona-agent
# The web component probe parses the recorded process command and refuses unrelated PIDs,
# wildcard hosts, and invalid ports instead of guessing at the dashboard default. Appended so
# every existing shard assignment remains unchanged.
smoke:web-probe-target
# The web runtime binds the requested remote host, rejects wildcard public URLs, and keeps the
# loopback default when no host is supplied. Appended so every existing shard assignment remains
# unchanged.
smoke:web-remote-bind
# The published manager entrypoint directly loads three NATS packages. Derive that runtime import
# closure from built dist and require every external package in it to be installed by a production
# dependency field. Appended so every existing shard assignment remains unchanged.
smoke:manager-runtime-deps
# Seat provenance rows must stay correct across the three ps presentations; appended so
# every existing shard assignment remains unchanged.
smoke:ps-provenance
# The real user-auth composition root owns a broker, Better Auth IdP, auth daemon, manager, and
# CLI subprocesses through grant, exchange, connect, revoke, crash-heal, and teardown. Appended so
# every existing shard assignment remains unchanged.
smoke:user-auth-launch:live
# `cotal up` on a root holding several tenants renders server.conf from the validated inventory: the
# MEMORY resolver is a whole-broker map, so a config rendered from the one booted space evicts every
# sibling silently. Drives the real CLI against a real broker. Appended so every existing shard
# assignment remains unchanged.
smoke:up-multi-space-render:live
# The $SYS store-injection eviction edge on a two-tenant live broker: the SecretStore-read
# credential pair must survive a store eviction without the delivery daemon re-reading a stale
# pair. Drives the real daemon against a real broker. Appended so every existing shard
# assignment remains unchanged.
smoke:sys-injection-evict
# A user-mode endpoint whose bearer expires must stop presenting the dead token: pre-fix it re-dialled
# it on a flat 3s interval forever, one client costing a permanent auth-callout round trip every 3s.
# Drives a real broker plus a real auth callout across three arms (no renewal source, healthy source,
# loud refusal). Appended so every existing shard assignment remains unchanged.
smoke:expiry-renewal:auth

# A RESUMED `cotal up` must render server.conf under the root maintenance lock, the way the ordinary
# and manifest paths do. The re-entry used to drop the recovery's lock and re-take it, leaving the
# journal naming a resume in flight with the root unlocked - an unserialized rewrite of every
# tenant's trust. Drives the real CLI against a real broker. Appended so every existing shard
# assignment remains unchanged.
smoke:up-resume-render-lock:live

# The other half of that lock hand-off: a resume re-entry INHERITS the recovery's lock, so a resume
# helper that self-acquires hits the "held by a live owner" refusal - the recorded owner is alive
# because it is us, which is also why stale-reaping cannot rescue it. Covers the two ADOPT paths,
# where an `up` recovers by adopting an already-live listener instead of spawning a competitor over
# the same store. Appended so every existing shard assignment remains unchanged.
smoke:up-adopt-resume-lock:live

# `up --restore` under that same inherited lock, and the more severe half: the restore-side journal
# writers self-acquired, so the FIRST of them - the listener bind - refused and the command did not
# run at all. The adopt suite above enters through a resume with no artifact and stays green through
# that break, so neither suite substitutes for the other. Appended; shard assignments unchanged.
smoke:up-restore-reentry:live

# The space-segmentation foundation (P7/P1 shared design). Hermetic and fast. It asserts the
# `spaceSegment` collision guarantee over the children of `.cotal/` — a WIDER namespace than the
# auth-dir siblings the encoder's comment originally claimed — plus the migration choke point's
# refusals and the `space add` door. Widened with the PER-KIND RESOLVERS: every kind moves on first
# touch through the one shared body, the hosted arm resolves the same key while moving nothing, the
# refusals travel OUT through the named wrappers, and the two non-migrating owners (the deleters and
# the renewal owner) address the same layout the resolvers do. Mutation-proofed —
# bin/smoke/mutations/space-segmentation.json. Appended; shard assignments unchanged.
smoke:space-segmentation

# The `$SYS` repair advice names only commands the root can run. The delivery daemon printed `cotal
# down` + `cotal up --rotate-sys` on multi-tenant roots, where both refuse - advice that cannot
# succeed, found by the P7 §6 probe and fixed alongside this suite. Hermetic and fast; the property
# is a biconditional, so the single-tenant arm is graded too. Appended; shard assignments unchanged.
smoke:sys-repair-honesty

# The per-agent standing secrets' half of that design (series P1). Hermetic and fast, and separate
# from the suite above because the kind set is OPEN - one file per agent per kind - so rule 2's "one
# rename" is per FILE here and the family (three secrets plus the non-secret health file the manager
# weighs with them) must move together. It also grades the one signature change in the series that is
# not bookkeeping: `agentSecretKeyForFile` takes the space from the CALLER's authority and CHECKS the
# path against it, so a recorded path cannot hand back a key into a co-resident tenant's material.
# And it holds the sweep to BOTH levels - every tenant's segment plus any file still flat - because a
# deleter that reported only the new level would strand an unmigrated root's creds and report
# success. Mutation-proofed - bin/smoke/mutations/agent-secret-segmentation.json. Appended; shard
# assignments unchanged.
smoke:agent-secret-segmentation
# Every workflow reachable by a push to main must QUEUE its runs rather than evict them. #976
# measured the eviction; #980 fixed ci.yml and three siblings carried the same defect in two
# spellings, one of them the release workflow. The property is only observable on main after a
# merge, so it needs a static guard. Appended so every existing shard assignment remains unchanged.
smoke:workflow-concurrency
# #878: a crash between the retirement barrier's gate terminal and its head terminal wedges the
# alias (the boot resume's gate-only owed-ness skipped the intent forever). The suite stages that
# window deterministically and proves BOTH repair triggers the issue's refined criteria demand:
# the next auth boot and the same-op rail re-request. Mutation-proofed -
# implementations/auth/smoke/mutations/retirement-wedge-resume.json. Appended; shard
# assignments unchanged.
smoke:retirement-wedge:auth

# Most smoke trees are still outside their package TypeScript projects. A type-only import from a
# workspace package can therefore name something the package never exported and vanish when tsx runs
# the suite. This compiler-API intersection checks every named workspace import against the package's
# public exports. Appended so every existing shard assignment remains unchanged.
smoke:workspace-import-exports

# A denied JetStream metadata read must reject the endpoint call, never return a well-formed partial
# result that callers mistake for genuine emptiness. A real narrowed JWT cred drives listChannels
# through a denied CHAT STREAM.INFO, paired with an actually absent CHAT-stream control.
# Mutation-proofed - bin/smoke/mutations/endpoint-permission-results.json. Appended so every existing
# shard assignment remains unchanged.
smoke:endpoint-permission-results:auth
# A stalled presence KV-watch must mark the observer view stale rather than sweep every
# peer offline. The endpoint cell holds the watch through a TCP proxy; the web cell drives
# the shipped presence-view → stale-pill path. Appended so every existing shard assignment
# remains unchanged.
smoke:presence-watch-stall
smoke:web-presence-view
# A sparse channelHistory must stop at the subject's first matching sequence, not stream
# sequence 1. Discriminator is drainWindow minStart, not the returned page. Appended so
# every existing shard assignment remains unchanged.
smoke:sparse-history-walk
# Raw NATS transport state is separate from full endpoint readiness. The deterministic suite grades
# epoch staleness and state contracts; its companion owns a throwaway broker and proves real
# disconnect/reconnect delivery. Appended so every existing shard assignment remains unchanged.
smoke:transport-liveness
smoke:transport-liveness:broker
# The connection status tool reaches MeshAgent state through a real in-memory MCP server and client,
# including a measured successful inbox drain. Appended so every existing shard assignment remains
# unchanged.
smoke:connection-status

# A directed DM arriving during a live Jcode turn must reach the recipient session through Jcode's
# soft-interrupt queue and commit only at the clean containing-turn boundary. Real loopback broker
# plus shipped host; mutation-proofed. Appended so every existing shard assignment remains unchanged.
smoke:jcode-mid-turn-delivery

# Mixed source, installed, and plugin generations must be self-identifying in the status Machine
# block. The installed cell enters through a package-manager-style symlink and the skills cell uses
# the real Claude plugin-list JSON seam. Mutation-proofed - bin/smoke/mutations/status-provenance.json.
# Appended so every existing shard assignment remains unchanged.
smoke:status-provenance
