---
"@cotal-ai/core": patch
---

Describe retry formats an arbitrary caught value without throwing, so a poisoned `message` getter or `toString` cannot escape the timer. The pending describe still settles as unavailable.
