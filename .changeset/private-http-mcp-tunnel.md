---
"@cotal-ai/mcp": minor
---

Add a loopback-only Streamable HTTP MCP transport for operator-run Secure MCP Tunnel connections. It enforces exact loopback routing, bounded initialize-only sessions, idle/LRU cleanup, and session-scoped identity retirement.
